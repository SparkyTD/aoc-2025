use std::fmt::Display;
use crate::utils::solution::Solution;

#[derive(Default)]
pub struct ProjectManagement;

impl Solution for ProjectManagement {
    fn solve(&self, input: String) -> (Box<dyn Display>, Box<dyn Display>) {
        todo!()
    }
}